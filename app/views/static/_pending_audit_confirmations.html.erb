<% @pendit_audit_confirmations.each do |pending_audit_confirmation| %>
  <%= link_to("I confirmed that I did not permorm any overtime for the week of:
    #{pending_audit_confirmation.start_date}", root_path, class: 'btn btn-lg
    btn-primary btn-block', data: { confirm: 'Are you sure you want to confirm that you did not perform any overtime?'}) %>
<% end %>
